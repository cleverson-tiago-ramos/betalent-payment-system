name: CI/CD BeTalent App

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 🔄 Clonar repositório
        uses: actions/checkout@v3

      - name: 🐳 Subir containers com Docker Compose
        run: docker compose up -d --build

      - name: ⏱️ Aguardar containers subirem
        run: sleep 15

      - name: 📜 Verificar logs iniciais
        run: docker compose logs app

      - name: 📦 Instalar dependências Laravel
        run: |
          docker compose exec app composer install --no-interaction --prefer-dist

      - name: 📝 Copiar .env padrão
        run: docker compose exec app cp .env.example .env || echo ".env já existe"

      - name: 🔐 Gerar APP_KEY
        run: docker compose exec app php artisan key:generate

      - name: 🗄️ Rodar migrations
        run: docker compose exec app php artisan migrate --force

      - name: ✅ Verificar containers em execução
        run: docker compose ps

      - name: 🧪 Executar testes Laravel
        run: docker compose exec app php artisan test

      - name: 🚀 Finalizar deploy
        run: echo "✅ Deploy concluído com sucesso!"
